# CLEAN machine section. Validates against schemas.py:ExecutionSection. HUMAN-REVIEWED.
# Routes `seqkit stats`: read-only summary stats to a TSV. -a = all stats, -T = tab-delimited.
argv: [seqkit, stats, -a, -T, -o, "{out_dir}/stats.tsv", "{input}"]
version_argv: [seqkit, version]
install_hint: "mamba install -c bioconda seqkit"
