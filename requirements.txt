# Python deps for both tracks. Install into the conda `nooa` env (which already provides nooa).
#
#   conda activate nooa
#   pip install -r requirements.txt
#
# nooa itself is installed separately (conda env `nooa`, package `nooa==0.0.8`).

# Pinned to the versions the framework comparison was validated against — the repo's
# CONCLUSIONS depend on framework behavior, so results must be reproducible.
# LangGraph track
langgraph==1.2.10
langchain-core==1.5.3

# shared layer
pyyaml==6.0.3
jsonschema==4.26.0
requests==2.34.2
pydantic==2.13.4

# External (not pip): FastQC 0.12.x  ->  mamba install -c bioconda fastqc
# External: a local Ollama server + a model (default OLLAMA_MODEL=qwen2.5vl:7b)
